// =====================================================================
// config.js - إعدادات البوت المحسنة
// =====================================================================

module.exports = {
    // =====================================================================
    // الإعدادات الأساسية
    // =====================================================================
    
    // معرف المطور (صاحب البوت)
    ownerId: 6707747395, // غيّر إلى معرف التليجرام الخاص بك
    
    // توكن بوت التليجرام
    telegramBotToken: "8918275970:AAHaLu1nKhvqMwnMvhXTGeIvMC_UqskIaFo", // ضع توكن البوت هنا
    
    // اسم الجلسة
    sessionName: "session",
    
    // معرف القناة (بدون @)
    chanelid: "apsanad70", // غيّر إلى معرف قناتك
    
    // معرف المجموعة (بدون @)
    chatgrupid: "sanadcrash", // غيّر إلى معرف مجموعتك
    
    // رابط الصورة الافتراضية
    thumburl: "https://i.imgur.com/default.jpg",
    
    // =====================================================================
    // إعدادات واتساب المحسنة
    // =====================================================================
    
    // مدة الاتصال بالواتساب (بالمللي ثانية)
    waConnectTimeout: 60000,
    
    // مدة إعادة المحاولة (بالمللي ثانية)
    waRetryDelay: 2000,
    
    // الحد الأقصى لمحاولات إعادة الاتصال
    waMaxRetries: 5,
    
    // تفعيل الوضع الهادئ
    waSilentMode: true,
    
    // مدة البقاء متصلاً
    waKeepAliveInterval: 30000,
    
    // =====================================================================
    // إعدادات الأمان
    // =====================================================================
    
    // مدة الكولدون الافتراضية بالدقائق
    defaultCooldown: 20,
    
    // الحد الأقصى لعدد المحاولات
    maxRetries: 3,
    
    // مدة انتظار إعادة الاتصال (بالملي ثانية)
    reconnectDelay: 2000,
    
    // تفعيل وضع الصيانة (true/false)
    maintenanceMode: false,
    
    // الحد الأقصى لعدد الجلسات النشطة
    maxSessions: 10,
    
    // تفعيل تسجيل الدخول التلقائي
    autoReconnect: true,
    
    // تفعيل الحماية من الهجمات
    antiSpam: true,
    
    // عدد الطلبات المسموحة في الدقيقة
    rateLimit: 30,
    
    // =====================================================================
    // إعدادات الميزات الجديدة
    // =====================================================================
    
    // تفعيل الرد التلقائي
    autoReply: true,
    
    // تفعيل رسائل الترحيب
    welcomeMessage: true,
    
    // تفعيل نظام الرتب
    rankSystem: true,
    
    // تفعيل الإحصائيات
    statsEnabled: true,
    
    // مدة تخزين السجلات (بالأيام)
    logRetention: 7,
    
    // تفعيل لوحة الأدمن
    adminPanel: true,
};